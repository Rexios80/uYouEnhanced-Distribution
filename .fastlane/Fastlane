platform :ios do
  lane :distribute do
      setup_ci if ENV['CI']

      build_ios_app(...)

      release = firebase_app_distribution(
          app: ENV["FIREBASE_APP_ID"],
          service_credentials_file: "firebase_app_distribution_service_account_key.json",
      )
  end
end